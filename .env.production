# Production build configuration.
#
# SAFE TO COMMIT. These two values are public by design: they are compiled
# into the JavaScript bundle that every visitor downloads, so they can never
# be secret. Row-level security on all 33 tables is what protects the data —
# this key on its own can read nothing, and signing in is still required.
#
# NEVER put the service_role / sb_secret_ key here. That one bypasses all
# security and belongs only in server-side settings.
VITE_SUPABASE_URL=https://fbnarookkrgmvferfgvy.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_8vtVB0IVqISVkjjuQoqsvA_xc5JR7t4
